#include <stdio.h>

/**
 * main - prints alpha in lower and upper case
 * Return: Always 0
 */
int main(void)
{
	char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
		putcher(ch);
	for (ch = 'A'; ch <= ;'Z';ch++)
			putcher(ch);
	putcher('\n');

	return (0);
}

